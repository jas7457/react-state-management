{
  "name": "react-state-management",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "client": "yarn workspace client"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  }
}
